#ifndef IPCONFIG_H
#define IPCONFIG_H

#include <QWidget>

class ipconfig : public QWidget
{
    Q_OBJECT
public:
    explicit ipconfig(QWidget *parent = nullptr);
    ~ipconfig();

signals:

public slots:

private:

};

#endif // IPCONFIG_H
